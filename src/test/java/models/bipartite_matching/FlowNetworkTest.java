/*
package models.bipartite_matching;

import models.Skill;
import models.Task;
import com.sun.javafx.tk.Toolkit;
import logic.FordFulkersonAlgorithm;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mockito;

import java.util.ArrayList;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;

*/
/**
 * Created by nura on 06/03/17.
 *//*

public class FlowNetworkTest {
    ArrayList<Task> tasks = new ArrayList();
    @Before
    public void setUp() throws Exception {
        int numberOfTasks = 5;
        int i=0;
        while (i<numberOfTasks) {
            Task t = Mockito.mock(Task.class);
            tasks.add(t);
            i++;
        }

    }

    @After
    public void tearDown() throws Exception {

    }

    @Test
    public void getMapFromSourceShouldReturnTaskMapForFF() {
        BipartiteGraph graph = new BipartiteGraph(FordFulkersonAlgorithm.class, tasks);
        FlowNetwork network = new FlowNetwork(graph);
        assertEquals(graph.getTaskMap(), network.getMapFromSource());
    }

}
*/
